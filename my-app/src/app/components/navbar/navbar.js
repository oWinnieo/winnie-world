import { RouterButton } from "../routerButton"
import './navbar.scss';
export const Navbar = () => {
    return (
        <div className="navbar">
            <div className="navbar-in">
                <RouterButton />
            </div>
        </div>
    )
}